




if (true) {
    let a = 10;
    const b = 20;
    var c = 30;
}

// console.log(a);
// console.log(b);
// console.log(c);


function one() {

    const username = "Pushkar"

    function two() {
        const web = "insta"
        // console.log(username)
    }


    // console.log(web)

    two();
}

one();


if (true) {
    const username = "pushkar"

    if (username === "pushkar") {
        const website = " youtube"
        // console.log(username + website)
    }

    // console.log(website);
}


// console.log(username);


//============================++++++++++++++++++++++++++++++++++++++++
console.log(addOne(5));
function addOne(num) {
    return num + 1;
}





addTwo(5)

/// function or called expresssion also 
const addTwo = function (num) {
    return num + 2
}


/// when function is declared by expression , it needs to be declared before it's use
/// Hoisting is JavaScript's default behavior of moving all declarations to the top of the current scope (to the top of the current script or the current function).

// ES6

